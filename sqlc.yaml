version: "2"
sql:
  - engine: "postgresql"
    queries:
      - "./internal/infrastructure/postgresql/sqlc/queries.sql"
    schema: "./internal/infrastructure/postgresql/migrations"
    gen:
      go:
        package: "repo"
        out: "./internal/infrastructure/postgresql/sqlc"
        sql_package: "pgx/v5"
        emit_json_tags: true
        emit_interface: true # generate Querier interface